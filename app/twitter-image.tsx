// X/Twitter falls back to og:image, but an explicit twitter:image lets card
// scrapers resolve without guessing. Shares the exact same generated asset.
export { default, alt, size, contentType } from "./opengraph-image";
